# Dependencies
node_modules/

# Build output
dist-build/
electron/renderer/dist/

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Local runtime config that may contain secrets (the app writes worker.env
# into the OS userData dir, never into the repo — but ignore stray copies)
*.env
!.env.example
worker.env
